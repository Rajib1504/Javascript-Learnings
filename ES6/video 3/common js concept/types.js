//1. java script is a dynamic programing language.
// 2.1 premetive data types:
// 2.2 Non premetive data types:
// non premetive data types are holding their reffernce

/*
8 ways to get undefined:
1.variable that is not initilized in this case we will get the undefined.
2.if any parameter didn't get the value then undefined error will ocare..
3.function with no return..
4.if return has nothing value in right side it  will show the error 
5.property that donsn't exists on an object will give you the error of undefined 
6.accessing array elemets outside of the index range
7.deleting an element inside and array
**type of undefind is undefined
*/
// if some value is none if you want to set it then use null;
// the type of null is an Object

// 2.1 premetive data types:
let a = "ram";
let n = 123;
let ispass = "flase";
// 2.2 Non premetive data types:
const arr = [12, 433, 534];
const obj = { name: "raj", education: "HS" };
// non premetive data types are holding their reffernce
let p = { myName: "kunal" };
let q = p;
console.log(p, q);
/*
8 ways to get undefined:
1.variable that is not initilized in this case we will get the undefined.
2.if any parameter didn't get the value then undefined error will ocare..
3.function with no return..
4.if return has nothing value in right side it  will show the error 
5.property that donsn't exists on an object will give you the error of undefined 
6.accessing array elemets outside of the index range
7.deleting an element inside and array
**type of undefind is undefined
*/
// if some value is none if you want to set it then use null;
// the type of null is an Object
